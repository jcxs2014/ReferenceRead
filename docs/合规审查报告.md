# 文献精读库合规审查报告

> **审查时间**：2026-08-15
> **审查范围**：38 篇文献的 `literature_analysis/` 目录，对照 `docs/READING_INSTRUCTIONS.md` 现行规范（§4 双路径选择、§3 结构树、§30 pages 卫生、§31 公式 LaTeX 化）
> **扫描率**：38/38 篇（100%），220 个分章文件
> **审查方法**：正则扫描全库分章文件 + 97 自查 + frontmatter 字段 + 公式格式

---

## 一、执行良好的项（✅ 合规）

| 规范项 | 检测结果 | 判定 |
|---|---|---|
| **pages 字段** | 38/38（100%） | ✅ 全量合规 |
| **H1 标题格式** | 38/38 为 `# 0. 文献基本信息` | ✅ 全量合规（`cc806cb` 统一后） |
| **Unicode 数学符号** | 全库 0 残留 | ✅ 批 2 扫尾彻底 |
| **路径 A 子节镜像** | 27/38 有 `### N.n.n` 子节结构 | ✅ 路径 A 覆盖率 71% |

## 二、合规差距（⚠️ 需关注）

### 差距 1：路径 B（八段模板）零执行

> **[WorkBuddy 批注 ② — 判定与差距 4 自相矛盾]**：报告差距 1 称"0/38 篇使用路径 B"，但差距 4 又列出 11 篇"无 `### N.n.n`"（正是路径 B 形态）。**路径 B 形态实际有 11 篇在用**（gabici/TA/giuffrida/cameron/kewley/dieterich 等短篇分章均为 `## N.N` 八段/0.x 格式）——准确的表述是"**存量路径 B 未按新规范（`X.1–X.8` 编号）重写**"，这与设计备忘 §3.2"存量不批量改造"原则一致，**不是"规范形同虚设"**。另注：TA/giuffrida/cameron 原文无编号章节（sections 已豁免，REVIEWS #30），其路径归属是**分章格式选择**，与 sections 字段无关。

**规范要求**：短篇原始论文（<20 页）→ 路径 B：`X.1–X.8` 八段模板（§4 路径选择表）

**实测**：**0/38 篇使用路径 B**。3 篇的老旧 `X.1–X.8` 片段（fowler、champagne、bertone 共 16 处）是历史遗留的旧格式，不是路径 B 规范执行。

**唯一明确候选**：cameron-1968（pages=1-19，<20 页，会议论文，3 章分章）。但其他短篇需判定：

| 篇目 | pages | 建议路径 | 理由 |
|---|---|---|---|
| cameron-1968 | 1-19 | **B** | 会议论文，<20 页 |
| grevesse-sauval-1998 | 133-139（7 页） | **A**（SSRv review） | 综述型期刊 |
| kewley-2001 | 121-140（20 页） | **A** 或 **B** | 原始论文，20 页边界 |
| dieterich-2014 | 94 | **B**（若 <20 页） | 单期号，需 PDF 页数确认 |
| telescope-array-2023 | 903-907（5 页） | **B** | Science 短文 |
| giuffrida-2022 | 4878（article-number） | **B** 或 **A** | 需 PDF 页数确认 |

**结论**：**路径 B 规范形同虚设**——要么是 3 篇短篇实际应走路径 A/未触达路径 B，要么是路径 B 从未被真正执行。

### 差距 2：中文翻译段大面积缺失

> **[WorkBuddy 批注 ① — 本项判定不成立，扫描方法有误]**：实测（2026-08-15 深夜）批 1 四篇**全部有翻译段**——ruszkowski 19 处、blasi 16 处、grenier 43 处、bhattacharjee 64 处；批 2 部分篇也有（如 cowan 09 分章 14 处、wallerstein 02 分章 14 处）。报告"24/27 路径 A 篇零翻译段"的根因：**扫描正则只匹配 `> **译文**` 前缀**，而库内实际格式是 `> **翻译内容**`（如 `> **宇宙线数密度估计**`）——后者**与 READING_INSTRUCTIONS §4 规范正文 L128/L134 完全一致**（`> **子节标题中文翻译**`，翻译内容直接加粗，无"译文"标签）。真实问题仅剩：规范 L140 的描述文字写"`> **译文**` 形式"与 L128 示例不一致（表述瑕疵，可统一为 `> **翻译内容**`），**非"翻译段缺失"**。**本项应降级为：格式表述需统一，覆盖无缺口。**

**规范要求**（§4 路径 A）：每个子节标题下第一行必须插入中文翻译段（`> **译文**`）

**实测**：**24/27 路径 A 篇（89%）零翻译段**。仅有 3 篇有翻译段（fowler 6 处、champagne 6 处、bertone 4 处），且这些是旧 `X.1–X.8` 格式残留，不是新 `> **译文**` 格式。

| 批次 | 篇数 | 翻译段 | 合规率 |
|---|---|---|---|
| 批 1（4 篇长综述） | 4 | 0 | **0%** |
| 批 2（20 篇） | 20 | 0 | **0%** |
| 旧格式路径 A | 3 | 3（旧格式） | 0% |

**结论**：**翻译段是路径 A 的核心要求，但批 1/批 2 均未执行**。这是本报告最严重的合规差距。

### 差距 3：97 子节级覆盖块覆盖率低

> **[WorkBuddy 批注 ③ — 属既定决策，非执行缺失]**：设计备忘 §5 明确 97 块"随补子节批次"（议题 3 落地时仅批 1 四篇为示范，见 REVIEWS #26）；批 2 未补 97 块是**拍板过的范围**（97 块随精读写入 / 按需拓展时补），非执行遗漏。报告结论 P2"不批量、按需补"自洽，标题"覆盖率低"定性偏重，建议改为"97 块按设计随按需拓展补充"。

**规范要求**：长文献的 `97_quality_check.md` 需补"子节级覆盖"块（原文子节名单 + ✅/❌ + 位置）

**实测**：**4/38 篇（10.5%）**——仅批 1 四篇（ruszkowski、bhattacharjee、blasi、grenier）。批 2 20 篇未补。

**结论**：**97 子节块未随批 2 推广**，覆盖率远低于预期。

### 差距 4：11 篇旧格式存量未升级

| 篇目 | pages | 类型 | 建议路径 | 现状 |
|---|---|---|---|---|
| strong-2007 | 2451-2489（39 页） | RMP 综述 | **A** | 4 分章，无 `### N.n.n` |
| mewaldt-2001 | 137-151（15 页） | SSRv 综述 | **A** | 5 分章，无 `### N.n.n` |
| hillas-1984 | 425-444（20 页） | ARA&A 综述 | **A** | 5 分章，无 `### N.n.n` |
| cowan-2021 | 015002 | RMP 综述 | **A** | 9 分章，无 `### N.n.n` |
| grevesse-sauval-1998 | 133-139（7 页） | SSRv 综述 | **A** | 1 分章，无 `### N.n.n` |
| biermann-1996 | 1-26（26 页） | 会议论文 | **A**（≥20 页） | 1 分章，无 `### N.n.n` |
| kewley-2001 | 121-140（20 页） | ApJ 原始 | **A** 或 **B** | 1 分章，无 `### N.n.n` |
| giuffrida-2022 | 4878 | Nature Comms | **B** | 4 分章，无 `### N.n.n` |
| telescope-array-2023 | 903-907（5 页） | Science | **B** | 5 分章，无 `### N.n.n` |
| dieterich-2014 | 94 | AJ | **B** | 1 分章，无 `### N.n.n` |
| cameron-1968 | 1-19（19 页） | 会议 | **B** | 3 分章，无 `### N.n.n` |

**结论**：11 篇为设计备忘 §3.2 明确不动的"存量"——**这不视为违规**（"存量仅当读到某篇觉得信息不够时按需拓展"）。但 strong-2007 作为 RMP 综述（39 页）是 01 域核心文献，建议优先标记为"按需拓展候选"。

---

## 三、总结与优先级建议

### 整体评分

| 维度 | 评分 | 说明 |
|---|---|---|
| 基础设施（pages/H1/Unicode） | ✅ **A 级** | 全量合规 |
| 路径 A 子节镜像 | ✅ **B+ 级** | 71% 覆盖率，缺翻译段 |
| 路径 B 八段模板 | ❌ **D 级** | 0% 执行 |
| 97 子节块 | ⚠️ **C 级** | 10.5% 覆盖率 |
| 翻译段 | ❌ **D 级** | 11% 覆盖率（旧格式） |

> **[WorkBuddy 批注 ⑤ — 评分修正（按批注①②③）]**：路径 B → ⚠️ **B- 级**（存量 11 篇为路径 B 形态，未按新规范重写属 §3.2 存量策略）；翻译段 → ✅ **A 级**（批 1/批 2 实质全覆盖，格式与规范一致，仅 L140 表述待统一）；97 子节块 → ⚠️ **C 级（按设计，随按需拓展补充，非缺陷）**。修正后整体：基础设施 A、路径 A B+、路径 B B-、翻译段 A、97 块 C（设计内）——**批 1/批 2 执行合规良好，无 P0 整改项**。

### 整改优先级（按影响面）

1. **P0（高优先级）**：中文翻译段——24 篇路径 A 缺 `> **译文**`，这是 §4 路径 A 的核心要求，影响可读性。批 1/批 2 均未执行，需统一补。
2. **P1（中优先级）**：路径 B 落地——确认 3–5 篇短篇的路径归属，选 1 篇示范后批量执行。
3. **P2（低优先级，不触发）**：97 子节块——只有当某篇被按需拓展时才补，不批量。
4. **P3（不触发）**：11 旧格式篇——按备忘 §3.2 原则不批量改造，仅当读者觉得信息不够时按需触发。

### 核心结论

**设计规范（路径 A/B + 翻译段 + 97 块）已落地于 READING_INSTRUCTIONS 和 quality_matrix，但批 1/批 2 执行的深度改造只覆盖了结构（子节镜像），未覆盖翻译段和 97 块——翻译段是最严重的执行缺口。** 路径 B 则从未被真正执行过，要么是短篇实际未触达路径 B 条件，要么是规范未被遵守。

> **[WorkBuddy 批注 ④ — 核心结论需按批注 ①② 修正]**：① "翻译段是最严重执行缺口"**不成立**——批 1/批 2 的翻译段实质全覆盖（格式 `> **翻译内容**` 与规范一致，仅 L140 表述待统一，见批注①）。② "路径 B 从未被真正执行"**表述不准**——存量 11 篇为路径 B 形态（见批注②），未按新规范重写是 §3.2 存量策略使然。**修正后结论**：批 1/批 2 执行**合规良好**（结构 + 翻译段 + 97 块示范均已落地）；真实待办仅：① READING_INSTRUCTIONS L140 翻译段格式表述统一 ② 存量路径 B 短篇按需拓展时按新规范执行（触发制）。

---

> **关联文档**：`docs/精读深度扩充设计备忘.md`（设计基点）、`docs/READING_INSTRUCTIONS.md` §4（规范文本）、`docs/REVIEWS.md` #25/#26（批 1/批 2 执行记录）
>
> ---
>
> *初始扫描与报告起草：DeepSeek Harness agent（2026-08-15）*
> *批注与修正：WorkBuddy（2026-08-15，commit `9f3675e`）*