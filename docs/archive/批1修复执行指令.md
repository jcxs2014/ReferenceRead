# 批 1 修复执行指令 — grenier/bhattacharjee 子节镜像遗留问题

> 生成：2026-08-15（WorkBuddy 主会话）｜执行：指定会话（本文件自包含）
> 背景：议题 3（97 子节级覆盖块）复验暴露批 1 遗留问题（覆盖率 grenier 7/13=53%、bhattacharjee 13/23=56%）——用户拍板一并安排修复
> 目标：① grenier 04/07 三级子节标题层级修正（`##` → `###` + 去尾点）② grenier 04 空译文占位符处理 ③ bhattacharjee §6.4/§6.12/§6.13 子节号对齐
> 注意：本任务与**批 2（执行中）并行**——只改 grenier/bhattacharjee 2 篇的分章，不碰批 2 涉及篇目

---

## 0. 待修项清单（实测确认）

### grenier-2015

**04_crwanderers.md**：
- `## 4.3.2. Populations of unresolved γ-ray sources and The Fermi Bubbles and Galactic Center`（L106）——三级误用二级 → **降为 `### 4.3.2`（去尾点）**
- `## 4. The Fermi Bubbles and Galactic Center`（L117）——错位重复标题 → 按原文结构修正（原文 4.3.2 下的内容？对照 PDF 确认，若为独立标题则改 `### 4.3.3` 或按原文）
- 空译文占位符：L8（`# 4. COSMIC-RAY WANDERERS...` 后）、L91（`## 4.3.` 后）等 `> **中文译文**` 无实际内容 → **补实际译文**（该标题的英文标题翻译）或删除空段（若为非子节标题）

**07_tracers.md**：
- `## 7.1.1. In the HI`（L34）、`## 7.1.2. In the dark neutral medium`（L42）、`## 7.1.3. About $X_{\text{CO}}$`（L68）、`## 7.1.4. On dust properties`（L86）——三级误用二级 → **降为 `### 7.1.1` / `### 7.1.2` / `### 7.1.3` / `### 7.1.4`（去尾点）**

**通用规则（grenier 全篇适用）**：
- `## N.n.n.`（三级带尾点）→ `### N.n.n`（降级 + 去尾点）；`## N.n.`（二级带尾点）→ `## N.n`（去尾点，与全库路径 A 一致）
- 三级标题下补/修正 `> **译文**` 段（实际译文，非占位符）

### bhattacharjee-sigl-2000

- `§6.4.1–6.4.6`、`§6.12.1–6.12.2`、`§6.13.1–6.13.2`：内容存在但 `###` 子节号与原文未对齐（用了别的标题格式）→ **按原文子节号对齐为 `### 6.4.1` 等**，标题沿用原文
- 定位：`grep -rnE "^#{2,3} 6\.(4|12|13)" 02_cosmic-ray-origins/0001_bhattacharjee-sigl-2000/literature_analysis/`（对照 fulltext 目录 L29 起的 6.x 层级）

## ⚠️ 铁律

1. **只改 grenier/bhattacharjee 2 篇的分章标题层级与子节号**——不动内容（FACT/公式/数值零改动）、不动 00_overview/97/98/99、不动 frontmatter
2. **标题文本沿用原文**（仅层级/尾点/编号修正；译文段补实际译文）
3. **不碰批 2 涉及篇目**（b2fh/trimble/bertone 等 20 篇）
4. 改完覆盖率应提升：grenier 7/13 → 13/13、bhattacharjee 13/23 → 23/23（若部分原文子节确实平铺未镜像，❌ 保留并注明）

## 1. 验证

```bash
# 1) 层级修正：grenier 04/07 无 `## N.n.n`（三级不得用二级）
grep -nE "^## [0-9]+\.[0-9]+\.[0-9]+" 02_cosmic-ray-origins/0006_grenier-2015/literature_analysis/04_crwanderers.md \
  02_cosmic-ray-origins/0006_grenier-2015/literature_analysis/07_tracers.md   # 应为空

# 2) 尾点：grenier 无 `### N.n.n.`（三级不带尾点）
grep -nE "^### [0-9]+\.[0-9]+\.[0-9]+\." 02_cosmic-ray-origins/0006_grenier-2015/literature_analysis/*.md  # 应为空

# 3) 空译文占位符：grenier 无孤立 `> **中文译文**`（无实际内容）
grep -rn "> \*\*中文译文\*\*" 02_cosmic-ray-origins/0006_grenier-2015/literature_analysis/ | head  # 应为空

# 4) 子节号对齐：bhattacharjee 6.4.1-6.4.6/6.12.1/6.12.2/6.13.1/6.13.2 均有 ### 且与原文一致
grep -rnE "^### 6\.(4|12|13)\.[0-9]" 02_cosmic-ray-origins/0001_bhattacharjee-sigl-2000/literature_analysis/ | wc -l  # 应 ≥9

# 5) 覆盖率复验：重跑 quality_matrix，grenier/bhattacharjee 覆盖率应显著提升
python3 scripts/quality_matrix.py | grep 子节镜像
```

## 2. 提交约定

- 2 个 commit（grenier / bhattacharjee 各一）：
  - `fix(papers): grenier-2015 子节标题层级修正（##→### + 去尾点）+ 空译文占位符处理`
  - `fix(papers): bhattacharjee-sigl-2000 子节号对齐（§6.4/§6.12/§6.13）`
- 精确 add 对应篇分章文件，**不得 git add -A**；不碰批 2 文件

## 3. 完成标准（WorkBuddy 复验口径）

1. grenier 04/07 无 `## N.n.n` 残留、无尾点三级标题、无空译文占位符
2. bhattacharjee §6.4.1-6.4.6/6.12.1-6.12.2/6.13.1-6.13.2 全部 `###` 对齐且标题沿用原文
3. quality_matrix 子节镜像行：grenier 覆盖率 → 13/13（或如实标注剩余 ❌ 原因）、bhattacharjee → 23/23（或如实）
4. 内容零改动（FACT 计数与 678138b 后一致）
5. 工作树干净（仅批 2 手册 untracked）、提交规范
