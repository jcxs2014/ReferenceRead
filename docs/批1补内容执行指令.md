# 批 1 补内容执行指令 — blasi-2013 / grenier-2015 丢失 FACT 归位

> **状态：✅ 复验确认误报（2026-08-15 深夜）**——WorkBuddy 修正归一化（去 LaTeX 标记/双反斜杠）+ 关键词语义验证后确认：7 个"缺失"特征全部已存在（LaTeX 行内/加粗 [FACT] 写法），grenier 01 段落语义完整保留；**无需补内容**。方法教训：FACT 零丢失判定须归一化 LaTeX + 关键词语义抽查，勿用字面量/前 N 字特征
> 生成：2026-08-15（WorkBuddy 主会话）｜执行：指定会话（本文件自包含）
> 背景：`678138b`（批 1 子节镜像）改造中，**blasi-2013 与 grenier-2015 丢失少量具体数值内容**（WorkBuddy 复验用数字特征双通道确认，非句子改写误报）
> 目标：把丢失的 FACT 内容**归位**到新版本对应子节，恢复信息零丢失
> **注意：`8329b6d` 已修复 23 篇 citations YAML 回归——本任务不得触碰 frontmatter/citations，只补正文 FACT**

---

## 0. 丢失清单（WorkBuddy 复验实测，对照 `678138b^` 旧版本）

### blasi-2013（2 条，均在 `02_sn_r_premises.md`）

| # | 缺失数值 | 旧版本原文（`git show 678138b^:02_cosmic-ray-origins/0004_blasi-2013/literature_analysis/02_sn_r_premises.md`） |
|---|---|---|
| B1 | `E_SN` | `- [FACT] 单次 SN 动能 E_SN = $10^{51}$·$E_{51}$ erg。` |
| B2 | `E_SN` | `- [FACT] 注入谱形式 N(p) = $\xi_{\rm CR}$·E_SN / m$^{2}$ · I($\gamma$) · (p/m)$^{-}$ᵞ，其中 I($\gamma$) ≈ 2(3−$\gamma$)($\gamma$−2)/(4−$\gamma$)...（以旧版本完整行为准）` |

### grenier-2015（5 条）

| # | 缺失数值 | 所在文件 | 旧版本原文线索 |
|---|---|---|---|
| G1 | `17 eV` | `01_scope_and_nine_lives.md` | 银河系 CR 加速至 knee（$3\times10^{15}$ eV）段的 FACT（旧版本 01 中"knee 能量处…谱 2.7 → 3.3"等叙述，含数值段落） |
| G2 | `20 K` | `05_crism_interactions.md` | `[FACT] 冷颗粒 T_dust <20 K → 挥发种（C、N、O、$H_{2}$O、$NH_{3}$、CO）冻结到表面 → 冰壳化学反应。` |
| G3 | `10^{9}` | `06_cr_stimuli.md` | `[FACT] $H_{2}^{\rm +}$ 可能在极端情况（积分 $\zeta_{H_{2}}^{\rm CR}$ × n($H_{2}$) dℓ = $\eta$ ≥ $10^{9}$ s$^{-1}$ cm$^{-2}$）在 IR/射电波长可探测...` |
| G4 | `400 K` | `06_cr_stimuli.md` | `[FACT] 不需气体高温（≥400 K），因为放热形成过程 $H_{2}$O$^{+}$ + $H_{2}$ → $H_{3}$O$^{+}$ + H 将化学反应能差转化为 $H_{3}$O$^{+}$ 内部（转动）能量（Lis 2014）。` |
| G5 | `10^{4}` | `07_tracers.md` | `[FACT] $10^{4}$–$10^{6}$ M⊙ 云样品中：...` |

**提取完整旧文**：
```bash
git show "678138b^:02_cosmic-ray-origins/0004_blasi-2013/literature_analysis/02_sn_r_premises.md" | grep -A5 "E_SN"
git show "678138b^:02_cosmic-ray-origins/0006_grenier-2015/literature_analysis/06_cr_stimuli.md" | grep -B2 -A4 "400 K"
```

## 1. 任务清单

### 步骤 1：定位新版本对应子节

用 grep 在新版本中找到语义对应的 `### N.n.n` 子节（B1/B2 → §2.2 源相关的子节；G2 → §5 CR-ISM 相关子节；G3/G4 → §6 CR 刺激相关；G1 → §1；G5 → §7 示踪相关）。找不到明确子节 → 归到该分章最相关子节或 `## N.n` 平铺区。

### 步骤 2：归位（保持路径 A 结构）

- 把旧版本 FACT **完整段落**（含公式/数值）插入对应子节内容区，保持 `[FACT]` 标签与 LaTeX 原样
- 不翻译成新句子、不压缩——**内容原样恢复**
- 若该子节已有同类内容但缺数值 → 合并补充数值，不重复整段

### 步骤 3：验证

```bash
# 1) 数字特征全恢复（复验脚本重跑应为 0 缺失）
#    blasi: E_SN 应 ≥1 处；grenier: 17 eV/20 K/10^{9}/400 K/10^{4} 各 ≥1 处
grep -c "E_SN" 02_cosmic-ray-origins/0004_blasi-2013/literature_analysis/*.md | grep -v ":0"
grep -c "400 K\|20 K\|10\^{9}\|10\^{4}" 02_cosmic-ray-origins/0006_grenier-2015/literature_analysis/*.md | grep -v ":0"

# 2) FACT 篇级计数 ≥ 旧版本（blasi ≥245 / grenier ≥338）
# 3) YAML 不受影响（00_overview 0 失败）
# 4) 反例不新增（无 ## § / 无占位符 / 无八段残留）
```

## ⚠️ 铁律

1. **只补正文 FACT 内容**——不触碰 frontmatter / citations / 00_overview / 97 / 98 / 99
2. **不新增分析**（只恢复旧内容归位）；不删减其他子节内容
3. 保持路径 A 结构（子节号/译文段不动）
4. 若某条在新版本已存在等价内容（如 G1 的 knee 段已在新 01 分章其他位置）→ **不重复添加**，在提交说明注明"已存在等价内容，跳过"

## 2. 提交约定

- **1 个 commit**（2 篇一起或分 2 个均可，建议分 2 个便于回溯）：
  - `fix(papers): blasi-2013 补回子节镜像丢失的 FACT（E_SN 等）`
  - `fix(papers): grenier-2015 补回子节镜像丢失的 FACT（400K/20K/10^9 等）`
- 只 add 对应篇的 `literature_analysis/*.md` 分章文件，**不得 git add -A**

## 3. 完成标准（WorkBuddy 复验口径）

1. blasi/grenier 数字特征缺失 = 0（复验脚本重跑）
2. FACT 篇级计数 ≥ 旧版本（blasi ≥245 / grenier ≥338）
3. YAML 全库 0 失败（frontmatter 未被触碰）
4. 反例不新增；路径 A 结构保持
5. 工作树干净、提交规范
